<!DOCTYPE html>
<html lang="it">
<head>
<meta charset="UTF-8">
<title>Vector — Log</title>
<style>
  body { font-family: system-ui, sans-serif; max-width: 640px; margin: 40px auto; padding: 0 16px; background:#0e0e10; color:#eee; }
  nav a { color:#3b82f6; margin-right:16px; text-decoration:none; }
  fieldset { border:1px solid #333; border-radius:8px; margin-bottom:16px; padding:12px 16px; }
  legend { padding:0 6px; color:#aaa; }
  label { display:block; margin:8px 0 4px; }
  input[type=text] { width:100%; padding:6px; border-radius:4px; border:1px solid #333; background:#1b1b1f; color:#eee; box-sizing:border-box; }
  button { margin-top:20px; padding:10px 16px; background:#3b82f6; color:white; border:none; border-radius:6px; cursor:pointer; }
  a { color:#3b82f6; }
</style>
</head>
<body>
  <nav>
    <a href="/">Home</a>
    <a href="/log">Log</a>
    <a href="/steps">Step successivi</a>
  </nav>

  <h1>Log — <%= today %></h1>
  <form method="POST" action="/log">
    <fieldset>
      <legend>Progetti</legend>
      <% config.projects.forEach(function(p) { %>
        <label>
          <input type="checkbox" name="project_<%= p.id %>_done" <%= (todayEntry.projects[p.id] && todayEntry.projects[p.id].done) ? 'checked' : '' %>>
          <%= p.name %> — step fatto oggi
        </label>
        <input type="text" name="project_<%= p.id %>_note" placeholder="nota (opzionale)" value="<%= (todayEntry.projects[p.id] && todayEntry.projects[p.id].note) || '' %>">
      <% }); %>
    </fieldset>
    <fieldset>
      <legend>Habit</legend>
      <% config.habits.forEach(function(h) { %>
        <label>
          <input type="checkbox" name="habit_<%= h.id %>" <%= (todayEntry.habits && todayEntry.habits[h.id]) ? 'checked' : '' %>>
          <%= h.name %>
        </label>
      <% }); %>
    </fieldset>
    <button type="submit">Salva</button>
  </form>
  <p><a href="/">&larr; torna alla home</a></p>
</body>
</html>
